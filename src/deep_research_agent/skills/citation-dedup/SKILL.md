# Citation Deduplication

Assign one citation number per unique URL across the final report. Reuse the
same number whenever the same URL supports multiple claims. End reports with a
Sources section listing citation number, title, and URL.

